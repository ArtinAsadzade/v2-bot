services:
  bot:
    build: .
    container_name: v2-bot
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "3000:3000"
    extra_hosts:
      - "host.docker.internal:host-gateway"
